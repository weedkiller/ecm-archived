@using DansLesGolfs.Base
@{
    Layout = "~/Views/_Shared/MP/_Admin.cshtml";
}
@section head
{
}
@section foot
{
    <script>
        $('.info-form').validate();
    </script>
}
<form method="post" id="form-@ViewBag.ClassName" class="info-form" enctype="multipart/form-data">
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        @Resources.GolfBrandInformation
                    </h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="form-group">
                            <div class="col-md-2 control-label">
                                <label for="GolfBrandName">@Resources.GolfBrandName</label>
                            </div>
                            <div class="col-md-10">
                                @Html.TextBox("GolfBrandName", (string)ViewBag.GolfBrandName, new { Required = "Required", maxlength = 100, @class = "form-control" })
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="form-group">
                            <div class="col-md-2 control-label">
                                <label for="GolfBrandDesc">@Resources.Description</label>
                            </div>
                            <div class="col-md-10">
                                @Html.TextArea("GolfBrandDesc", (string)ViewBag.GolfBrandDesc, new { Rows = 5, maxlength = 4000, @class = "form-control" })
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="form-group">
                            <div class="col-md-2 control-label">
                                <label for="GolfBrandImage">@Resources.Logo</label>
                            </div>
                            <div class="col-md-10">
                                @if(ViewBag.GolfBrandImage != null && ViewBag.GolfBrandImage != "")
                                {
                                    <img src="@Url.Content("~/Uploads/GolfBrands/" + ViewBag.GolfBrandImage)" />
                                }
                                else
                                {
                                    <img src="@Url.Content("~/Assets/Front/img/no-image-200x100.png")" />
                                }
                                <input type="file" id="GolfBrandImage" name="GolfBrandImage" accept="gif,png,jpg,jpeg,svg" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <input type="hidden" name="id" value="@ViewBag.id" />
    <input type="submit" id="submit" name="submit" value="@Resources.Save" class="btn btn-primary" />
    <input type="reset" id="reset" name="reset" value="@Resources.Reset" class="btn" />
    <a href="@Url.Content("~/Admin/GolfBrand")" class="btn btn-default">@Resources.Back</a>
</form>